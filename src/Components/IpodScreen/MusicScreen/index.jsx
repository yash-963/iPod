const MusicScreen = () => {
  return (
    <div
      style={{
        display: "flex",
        background: "#fff",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      Music Screen
    </div>
  );
};

export default MusicScreen;
