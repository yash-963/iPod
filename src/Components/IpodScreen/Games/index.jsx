import SportsEsportsIcon from "@mui/icons-material/SportsEsports";

const GameScreen = () => {
  return (
    <div
      style={{
        display: "flex",
        background: "#fff",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      <SportsEsportsIcon sx={{ fontSize: "40px" }} /> Games
    </div>
  );
};

export default GameScreen;
