const CoverFlow = () => {
  return (
    <div
      style={{
        display: "flex",
        background: "#fff",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      Coverflow
    </div>
  );
};

export default CoverFlow;
